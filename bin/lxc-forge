#!/usr/bin/env ruby
require 'lxc_forge'

options = LxcForge::Options.parse(ARGV)

begin
  LxcForge::Command.new(options).run
rescue Exception => e
  if options[:verbose]
    raise e
  else
    $stderr.puts e.message
    exit 1
  end
end
